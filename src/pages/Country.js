import { useParams } from "react-router-dom"
export default function Country({countries}){

    // const {id} = useParams()

    
    // const country = countries.filter(country => parseInt(country.id) === parseInt(id))
    // console.log(country)

    return(
        <>
            <h1>Country</h1>
        </>
    )
}